/*Etkinliklerle aynı yapı ama proje alanlarıyla:

Liste + filtre (komite/tag/durum)

Proje ekle / düzenle / sil (projects.*)

Scope: komite lideri sadece kendi komitesinde */
export function renderProjects() {
  return `
    <section class="page projects-page">
      <h1>Projeler</h1>
    </section>
  `;
}